---
name: Button
menu: 1. Atoms
---

import { Playground, Props } from "docz"
import Button from "./Button.jsx"
import Picto from "../Picto"

# Button

## Utilisation

### Simple

<Playground>
  <Button type="button" color="primary">
    This is a button
  </Button>
</Playground>

### Outline

<Playground>
  <Button
    type="button"
    color="primary"
    outline
    onClick={() => console.log("action")}
  >
    This is an outline button
  </Button>
</Playground>

### Icon

<Playground>
  <Button type="button" color="primary" onClick={() => console.log("action")}>
    <Picto icon="user" style={{ fill: "var(--color-white)", height: "16px" }} />
    This is an button
  </Button>
</Playground>

### Small

<Playground>
  <Button
    type="button"
    color="primary"
    size="s"
    onClick={() => console.log("action")}
  >
    This is a small button
  </Button>
</Playground>

### Big

<Playground>
  <Button
    size="l"
    type="button"
    color="primary"
    onClick={() => console.log("action")}
  >
    <Picto icon="user" style={{ fill: "var(--color-white)", height: "16px" }} />
    This is a big icon button
  </Button>
</Playground>

### Loader

<Playground>
  <Button
    size="m"
    type="button"
    isLoading={true}
    color="primary"
    onClick={() => console.log("action")}
  >
    This is a loading button
  </Button>
</Playground>

### Disabled

<Playground>
  <Button
    size="m"
    type="button"
    disabled={true}
    color="primary"
    onClick={() => console.log("action")}
  >
    This is a disabled button
  </Button>
</Playground>

## Props

<Props of={Button} />
